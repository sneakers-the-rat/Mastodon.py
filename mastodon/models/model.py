from pydantic import BaseModel

class MastodonModel(BaseModel):
    """
    Just an empty base class to inherit from for now, but
    could hold any common class methods/attrs. that might
    be needed.
    """